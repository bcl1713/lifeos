# LifeOS operations

This document will be expanded with the verified Portainer stack name, TrueNAS bind mounts, proxy target, backup commands, restore procedure, and rollback steps before production deployment.

Do not deploy from this application repository directly. Production deployment is Git-backed through `bcl1713/homelab-stacks`.
